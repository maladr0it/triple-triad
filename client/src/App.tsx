import React, { useCallback } from "react";

import "./App.css";

import { Board } from "./components/Board";

export const App: React.FC = () => {
  return (
    <div>
      <Board />
    </div>
  );
};
